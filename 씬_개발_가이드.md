# 씬 개발 가이드

이 문서는 Unity 프로젝트의 5개 씬을 개발하는 방법을 단계별로 설명합니다.

## 📋 씬 목록
1. **Entry Scene** - 게임 시작 화면
2. **Bunker** - 안전한 기지 (NPC들이 있는 곳)
3. **City** - 외부 도시 (좀비가 있는 곳)
4. **InBuilding** - 건물 내부 (상호작용 가능한 곳)
5. **Transition Scene** - 씬 전환 로딩 화면

---

## 🎯 1. Entry Scene (시작 화면)

### 목적
- 게임 시작 화면
- 메인 메뉴 표시
- 게임 시작 버튼

### 개발 단계

#### Step 1: 기본 씬 설정
1. Unity에서 `Entry Scene` 열기
2. Hierarchy에 빈 GameObject 생성 → 이름: "UI Manager"
3. Canvas 생성 (UI > Canvas)
   - Canvas Scaler 설정: Scale With Screen Size
   - Reference Resolution: 1920 x 1080

#### Step 2: 메인 메뉴 UI 구성
1. Canvas 하위에 Panel 생성 (UI > Panel) → 이름: "Main Menu Panel"
2. Panel 하위에 다음 UI 요소들 추가:
   - **Text (TMP)** → 이름: "Title Text"
     - 내용: "게임 제목"
     - 폰트 크기: 72
     - 정렬: 중앙
   - **Button** → 이름: "Start Button"
     - 텍스트: "게임 시작"
   - **Button** → 이름: "Settings Button" (선택사항)
     - 텍스트: "설정"
   - **Button** → 이름: "Quit Button"
     - 텍스트: "종료"

#### Step 3: 시작 버튼 스크립트 연결
1. "Start Button" 선택
2. Inspector에서 OnClick() 이벤트 추가
3. "UI Manager" 오브젝트에 새 스크립트 추가: `EntrySceneManager.cs`
4. OnClick()에 `EntrySceneManager.StartGame()` 연결

#### Step 4: 배경 설정 (선택사항)
1. 빈 GameObject 생성 → 이름: "Background"
2. Sprite Renderer 컴포넌트 추가
3. 배경 이미지 할당

---

## 🏠 2. Bunker Scene (벙커)

### 목적
- 안전한 기지
- NPC들과 대화
- 침대에서 휴식
- 도시로 나가는 출구

### 개발 단계

#### Step 1: 기본 씬 설정
1. Unity에서 `Bunker` 씬 열기
2. Hierarchy에 필수 시스템 오브젝트 확인/생성:
   - **Game Manager** (GameManager 스크립트)
   - **Time System** (TimeOfDaySystem 스크립트)
   - **Ending Manager** (EndingManager 스크립트)
   - **Chat System** (ChatSystem 스크립트)
3. 빈 GameObject 생성 → 이름: "Scene Initializer"
   - `SceneInitializer` 스크립트 추가
   - Auto Initialize: 체크
   - Player Spawn Point: 플레이어 시작 위치 Transform 할당 (선택사항)

#### Step 2: 플레이어 배치
1. Player 프리팹을 Hierarchy로 드래그
2. 시작 위치 설정 (예: (0, 0, 0))
3. Player에 다음 컴포넌트 확인:
   - `PlayerController_M` 또는 `PlayerController`
   - `PlayerStats`
   - `ChatWithNPC`
   - `Rigidbody2D`
   - `Collider2D` (CircleCollider2D 또는 BoxCollider2D)

#### Step 3: 카메라 설정
1. Main Camera 선택
2. `CameraFollow` 스크립트 추가 (플레이어를 따라다니는 카메라)
3. Target 필드는 비어두면 자동으로 Player를 찾습니다
4. Follow Speed: 2 (부드러운 이동)
5. Offset: (0, 0, -10) (Z축 거리)

#### Step 4: NPC 배치
각 NPC는 빈 GameObject로 생성하고 다음 태그와 컴포넌트를 추가:

**식품 연구 NPC:**
1. GameObject 생성 → 이름: "FoodResearch NPC"
2. Tag: "FoodResearch"
3. Sprite Renderer 추가 (NPC 스프라이트 할당)
4. Collider2D 추가 (Is Trigger 체크)
5. `FoodResearch` 스크립트 추가

**종말론자 NPC:**
1. GameObject 생성 → 이름: "Doomsday NPC"
2. Tag: "Doomsday"
3. Sprite Renderer 추가
4. Collider2D 추가 (Is Trigger 체크)
5. `DoomsDay` 스크립트 추가

**과학자 NPC (선택사항):**
1. GameObject 생성 → 이름: "Scientist NPC"
2. Tag: "Scientist"
3. Sprite Renderer 추가
4. Collider2D 추가 (Is Trigger 체크)

**시민 NPC (선택사항):**
1. GameObject 생성 → 이름: "Civilian NPC"
2. Tag: "Civilian"
3. Sprite Renderer 추가
4. Collider2D 추가 (Is Trigger 체크)

#### Step 5: 침대 배치
1. GameObject 생성 → 이름: "Bed"
2. Sprite Renderer 추가 (침대 스프라이트)
3. Collider2D 추가 (Is Trigger 체크)
4. `BedInteractable` 스크립트 추가
5. `IInteractable` 인터페이스 구현 확인

#### Step 6: 씬 전환 포인트 (도시로 나가기)
1. 빈 GameObject 생성 → 이름: "Exit to City"
2. Transform 위치: 벙커 출구 위치
3. BoxCollider2D 추가
   - Is Trigger: 체크
   - Size: 적절한 크기 (예: 2 x 2)
4. `SceneTransitionTrigger` 스크립트 추가
   - Target Scene Name: "City"
   - Use Transition Scene: 체크
   - Requires Interaction: 체크 (E키로 이동)
   - Interaction Prompt: "E키를 눌러 도시로 이동"

#### Step 7: 배경 타일맵 설정 (선택사항)
1. Tilemap 생성 (2D Object > Tilemap > Rectangular)
2. 타일 팔레트 생성 및 타일 배치

---

## 🏙️ 3. City Scene (도시)

### 목적
- 외부 도시 환경
- 좀비와의 전투
- 건물 입장 포인트
- 벙커로 돌아가는 포인트

### 개발 단계

#### Step 1: 기본 씬 설정
1. Unity에서 `City` 씬 열기
2. 필수 시스템 오브젝트 확인:
   - **Game Manager**
   - **Time System**
   - **Ending Manager**
3. 빈 GameObject 생성 → 이름: "Scene Initializer"
   - `SceneInitializer` 스크립트 추가
   - Auto Initialize: 체크

#### Step 2: 플레이어 배치
1. Player 프리팹 배치
2. 시작 위치 설정 (벙커 출구 근처)

#### Step 3: 카메라 설정
1. Main Camera에 `CameraFollow` 스크립트 추가
2. Target은 자동으로 찾거나 Player를 직접 할당

#### Step 4: 좀비 스폰 시스템
1. GameObject 생성 → 이름: "Zombie Spawner"
2. `ZombieSpawner_Test` 스크립트 추가
3. 스폰 위치 설정 (여러 개의 Spawn Point 생성 가능)

#### Step 5: 좀비 프리팹 배치 (테스트용)
1. Zombie 프리팹을 Hierarchy로 드래그
2. `ZombieAI` 스크립트 확인
3. Tag: "Zombie" 확인

#### Step 6: 씬 전환 포인트들

**벙커로 돌아가기:**
1. GameObject 생성 → 이름: "Return to Bunker"
2. 위치: 벙커 입구 근처
3. BoxCollider2D 추가 (Is Trigger)
4. `SceneTransitionTrigger` 추가
   - Target Scene Name: "Bunker"
   - Use Transition Scene: 체크
   - Requires Interaction: 체크

**건물 입장:**
1. GameObject 생성 → 이름: "Enter Building"
2. 위치: 건물 입구
3. BoxCollider2D 추가 (Is Trigger)
4. `SceneTransitionTrigger` 추가
   - Target Scene Name: "InBuilding"
   - Use Transition Scene: 체크
   - Requires Interaction: 체크
   - Interaction Prompt: "E키를 눌러 건물 입장"

#### Step 7: 배경 및 환경
1. 타일맵으로 도시 배경 구성
2. 장애물 배치 (Collider2D 추가)
3. 조명 설정 (2D Light 사용 가능)

---

## 🏢 4. InBuilding Scene (건물 내부)

### 목적
- 건물 내부 환경
- 상호작용 가능한 오브젝트
- 전리품 수집
- 도시로 나가는 출구

### 개발 단계

#### Step 1: 기본 씬 설정
1. Unity에서 `InBuilding` 씬 열기
2. 필수 시스템 오브젝트 확인
3. 빈 GameObject 생성 → 이름: "Scene Initializer"
   - `SceneInitializer` 스크립트 추가
   - Auto Initialize: 체크

#### Step 2: 플레이어 배치
1. Player 프리팹 배치
2. 시작 위치: 건물 입구

#### Step 3: 카메라 설정
1. Main Camera에 `CameraFollow` 스크립트 추가
2. Target은 자동으로 찾거나 Player를 직접 할당

#### Step 4: 상호작용 오브젝트 배치

**전리품 상자:**
1. GameObject 생성 → 이름: "Lootable Chest"
2. Sprite Renderer 추가
3. Collider2D 추가 (Is Trigger)
4. `Lootable` 스크립트 추가

**기타 상호작용 오브젝트:**
- `IInteractable` 인터페이스를 구현한 스크립트 추가

#### Step 5: 씬 전환 포인트 (도시로 나가기)
1. GameObject 생성 → 이름: "Exit to City"
2. 위치: 건물 출구
3. BoxCollider2D 추가 (Is Trigger)
4. `SceneTransitionTrigger` 추가
   - Target Scene Name: "City"
   - Use Transition Scene: 체크
   - Requires Interaction: 체크

#### Step 6: 배경 설정
1. 타일맵으로 건물 내부 구성
2. 벽, 바닥, 천장 타일 배치

---

## 🔄 5. Transition Scene (전환 씬)

### 목적
- 씬 전환 시 로딩 화면
- 진행률 표시

### 개발 단계

#### Step 1: 기본 씬 설정
1. Unity에서 `Transition Scene` 열기
2. Hierarchy에 빈 GameObject 생성 → 이름: "Transition Manager"
3. `TransitionSceneManager` 스크립트 추가 확인

#### Step 2: UI 구성
1. Canvas 생성
2. Canvas 하위에 Panel 생성 → 이름: "Loading Panel"
3. Panel 하위에 다음 추가:
   - **Image** → 이름: "Progress Bar Background"
     - Color: 어두운 색상
   - **Image** → 이름: "Progress Bar Fill"
     - Image Type: Filled
     - Fill Method: Horizontal
     - Color: 밝은 색상
     - Progress Bar Background의 자식으로 배치

#### Step 3: 스크립트 연결
1. "Transition Manager" 선택
2. `TransitionSceneManager` 컴포넌트 확인
3. Progress Image 필드에 "Progress Bar Fill" 할당

#### Step 4: 배경 설정 (선택사항)
1. 배경 이미지 또는 색상 설정
2. 로딩 텍스트 추가 (선택사항)

---

## 🔗 씬 전환 흐름도

```
Entry Scene
    ↓ (게임 시작)
Bunker ←→ City ←→ InBuilding
    ↑         ↓
    └─────────┘
    (모든 전환은 Transition Scene을 거침)
```

---

## ✅ 체크리스트

각 씬 개발 후 확인사항:

- [ ] 플레이어가 씬에 배치되어 있는가?
- [ ] 카메라가 플레이어를 따라다니는가?
- [ ] 씬 전환 포인트가 올바르게 설정되어 있는가?
- [ ] 필수 시스템 오브젝트들이 있는가?
- [ ] NPC들이 올바른 태그와 스크립트를 가지고 있는가?
- [ ] Collider2D가 Is Trigger로 설정되어 있는가?
- [ ] 씬 이름이 정확한가? (대소문자 구분)

---

## 🐛 문제 해결

### 씬 전환이 안 될 때
1. 씬 이름이 정확한지 확인 (대소문자 포함)
2. 씬이 Build Settings에 추가되어 있는지 확인
3. `SceneTransitionTrigger`의 Target Scene Name 확인
4. PlayerPrefs "NextKey" 값 확인

### 플레이어가 씬에 나타나지 않을 때
1. Player 프리팹이 Hierarchy에 있는지 확인
2. Player의 Transform 위치 확인
3. Player의 스크립트들이 올바르게 설정되어 있는지 확인

### NPC와 상호작용이 안 될 때
1. NPC의 Tag가 올바른지 확인
2. Collider2D가 Is Trigger로 설정되어 있는지 확인
3. `ChatWithNPC` 스크립트가 Player에 있는지 확인

---

## 📝 다음 단계

씬 개발이 완료되면:
1. Build Settings에 모든 씬 추가
2. 각 씬에서 플레이 테스트
3. 씬 전환이 부드럽게 작동하는지 확인
4. NPC 대화 시스템 테스트
5. 엔딩 조건 테스트

